# Ignore SSH private keys
*.pem
*.pub
*.key
id_rsa
id_dsa
id_ecdsa
id_ed25519
# Also ignore any files named like ssh keys
ssh_key
ssh_key.*

# Ignore Terraform state files and backups
*.tfstate
*.tfstate.*
.terraform/
terraform.tfstate
terraform.tfstate.backup

# Ignore Terraform variables files that may contain secrets
*.tfvars
*.tfvars.json

# Ignore crash logs from Terraform
crash.log
